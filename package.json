{
  "name": "stripe-gradient",
  "version": "1.0.0",
  "author": "Kyle Hasty <contact@kylehasty.com>",
  "description": "Stripe WebGl Gradient Animation",
  "repository": {
    "type": "git",
    "url": "https://github.com/khasty720/stripe-gradient.git"
  },
  "keywords": ["WebGl", "Stripe", "gradient"],
  "license": "MIT",
  "source": "src/index.js",
  "main": "dist/main.js",
  "scripts": {
    "watch": "parcel watch",
    "build": "parcel build",
    "version:prerelease": "yarn version --prerelease --preid canary",
    "publish:prerelease": "yarn run build && yarn run login && yarn publish --tag canary"
  },
  "devDependencies": {
    "eslint": "^8.11.0",
    "eslint-config-airbnb": "^18.2.1",
    "eslint-plugin-import": "^2.25.2",
    "parcel": "^2.8.2"
  }
}
